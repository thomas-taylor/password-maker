<UserControl x:Class="PasswordMakerWpf.IconButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d" d:DesignHeight="50" d:DesignWidth="150">
    <Button Click="ButtonClick">
        <StackPanel Orientation="Horizontal">
            <materialDesign:PackIcon Kind="{Binding PackIconKind}" x:Name="packIcon"
                VerticalAlignment="Center" Width="20" Height="20"/>
            <TextBlock Text="{Binding Caption}" x:Name="textBlock"
                VerticalAlignment="Center" Margin="3,1,0,0" />
        </StackPanel>
    </Button>
</UserControl>